import { TextStyle, ViewStyle } from 'react-native'

export const tableOrderStyles = {
  title: {
    fontSize: 20,
  } as TextStyle,
  priceWrapper: {
    paddingBottom: 20,
    alignItems: 'center',
  } as ViewStyle,
}
