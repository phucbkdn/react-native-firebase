
import { TextStyle, ViewStyle } from 'react-native'

export const discountStyles = {
  container: {
    flex: 1,
  } as ViewStyle,
  title: {
    fontSize: 20,
    fontWeight: 'bold',
  } as TextStyle,
  priceWrapper: {
    flexDirection: "row",
    paddingBottom: 20,
  } as ViewStyle,
}
